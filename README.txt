# CPAS虛擬個案晤談蒐證模擬器

## 使用方式
1. 解壓縮 ZIP。
2. 直接用 Chrome 或 Edge 開啟 `index.html`。
3. 不需要伺服器、不需要網路、不需要登入。

## 教學流程
- 第一階段：盲蒐證，不顯示 CPAS，最多問 10 題。
- 第二階段：揭示 CPAS，回頭對照晤談資料與分數。
- 第三階段：整理報告骨架。

## 可調整內容
- 題庫在 `data.js`。
- 題數限制在 `app.js` 的 `MAX_QUESTIONS`。
- 時間在 `app.js` 的 `seconds:900`，900秒等於15分鐘。
